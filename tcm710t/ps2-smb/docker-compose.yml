services:
  ps2-smb:
    container_name: ps2-smb
    image: fortu/ps2smb
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./config:/etc/samba:ro
      - games:/games:rw

volumes:
  games:
    name: ps2_smb_games
    driver: local
    driver_opts:
      type: cifs
      device: //${SMB_HOST}/${SMB_SHARE}
      o: username=${SMB_USERNAME},password=${SMB_PASSWORD},vers=3.0,rw,noatime